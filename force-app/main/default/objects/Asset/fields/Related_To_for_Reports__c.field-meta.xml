<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Related_To_for_Reports__c</fullName>
    <description>This field is created for the purpose of Reporting to prevent hyperlink tag in legends</description>
    <externalId>false</externalId>
    <formula>IF(NOT(ISBLANK(Projects__c)),Projects__r.Name,
IF(NOT(ISBLANK(Event__c)),Event__r.Name,IF(NOT(ISBLANK(Training__c)),Training__r.Name,&quot;&quot;)))</formula>
    <label>Related To</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
